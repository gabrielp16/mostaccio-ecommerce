function Footer() {
  return (
    <footer className="site-footer">
      <div className="container d-flex flex-column flex-md-row justify-content-between gap-2 text-uppercase small fw-semibold">
        <span>Diseno WebUI enfocado en conversion</span>
        <span>React + MongoDB + Bootstrap</span>
      </div>
    </footer>
  )
}

export default Footer
